# Config
# ========================================================================================

# Site URL
# 如果你的 GitHub Page 是 https://itribgc.github.io/
# 建議填這個：
url:                   https://itribgc.github.io

# 個人 GitHub Page 通常 baseurl 要留空
# 例如 https://itribgc.github.io/ → baseurl: ""
# 如果是專案頁面，例如 https://itribgc.github.io/my-site/ 才需要 baseurl: "/my-site"
baseurl:               ""


# General settings
# ---------------------------------------------------------------------------------------

# Language of your content
lang:                  zh-TW

# The title of your blog. Used in the sidebar and the browser tab.
title:                 Starter Kit

# A short description used for the meta description tag.
description:           >
  Hydejack is a boutique Jekyll theme for hackers, nerds, and academics,
  with a focus on personal sites that are meant to impress.

# A shorter description for the sidebar.
tagline:               Change `tagline` in `_config.yml`

# A list of keywords for your blog
keywords:              []

# A square logo for your site.
logo:                  /assets/img/logo.png

# This should be the same author as first entry in `_data/authors.yml`.
author:
  name:                <firstname> <lastname>
  email:               <mail@domain.tld>
  # twitter:             <username>

# Add links to the sidebar.
menu:
  - title:             Example
    url:               /example/
  - title:             Documentation
    url:               /docs/
  - title:             About
    url:               /about/

# Add links to the footer.
legal:
  - title:             LICENSE
    url:               /LICENSE/
  - title:             NOTICE
    url:               /NOTICE/
  - title:             CHANGELOG
    url:               /CHANGELOG/

# This text will appear in a footer of every page.
copyright:             © 2024. All rights reserved.

# Format of the permalinks
permalink:             /:categories/:year-:month-:day-:title/

# Pagination configuration
paginate:              10
paginate_path:         /:num/


# Theme
# ---------------------------------------------------------------------------------------

# GitHub Pages 預設 build 找不到 jekyll-theme-hydejack，
# 所以這裡改用 remote_theme。
# theme: jekyll-theme-hydejack
remote_theme:          hydecorp/hydejack@v9


# Customization
# ----------------------------------------------------------------------------------------

# Sidebar image and theme color of the site.
accent_image:          /assets/img/sidebar-bg.jpg
accent_color:          rgb(79,177,186)

# Browser UI theme color.
theme_color:           rgb(25,55,71)

# Google Fonts are turned off by default.
# google_fonts:          Roboto+Slab:700|Noto+Sans:400,400i,700,700i

# font:                  Noto Sans, Helvetica, Arial, sans-serif
# font_heading:          Roboto Slab, Helvetica, Arial, sans-serif
# font_code:             Fira Code, Menlo, Monaco, Consolas, monospace;


defaults:
  # You can use this to provide a default accent color and background for
  # all pages under a given path:
  - scope:
      path:            hyde/
    values:
      accent_color:    rgb(38,139,210)
      theme_color:     rgb(32,32,32)
      accent_image:
        background:    rgb(32,32,32)
        overlay:       false

  # Don't include documents in assets in the sitemap
  - scope:
      path:            assets/
    values:
      sitemap:         false

  # Don't include licenses in sitemap
  - scope:
      path:            licenses/
    values:
      layout:          plain
      sitemap:         false

  # `jekyll-seo-tag` will mark collection outputs as `BlogPosting`,
  # but we want them to be `WebPage`s:
  - scope:
      type:            projects
    values:
      seo:
        type:          WebPage

  - scope:
      type:            featured_categories
    values:
      seo:
        type:          WebPage

  - scope:
      type:            featured_tags
    values:
      seo:
        type:          WebPage


kramdown:
  # GitHub Pages 不支援 kramdown-math-katex，
  # 所以這裡改成 mathjax，避免 build 因 KaTeX gem 失敗。
  math_engine:         mathjax
  math_engine_opts:    {}
  footnote_backlink:   '&#x21a9;&#xfe0e;'


# Hydejack Settings
# ----------------------------------------------------------------------------------------

hydejack:
  # Configure the order of complementary content on blog posts
  post_addons:         [about, newsletter, related, random, comments]

  # Configure the order of complementary content on project pages
  project_addons:      [about, newsletter, other, comments]

  # Set to `true` if you don't want to show an icon indicating external links
  no_mark_external:    false

  # Set to `true` if third party plugins fail to work with dynamically loaded pages
  no_push_state:       false

  # Set to `true` if you want to disable the drawer
  no_drawer:           false

  # Set to `true` if you don't want to use the auto-hiding navbar.
  no_navbar:           false

  # Set to true to disable the built-in search functionality.
  no_search:           false

  # Set to `true` if you do not want parts of the css inlined in <head/>
  no_inline_css:       false

  # Set to `true` if you don't intend on changing the accent color on a per-page basis.
  no_page_style:       false

  # Code blocks and tables "break" the layout by spanning the full available width.
  no_break_layout:     true

  # Set to `true` to disable the dynamic Table of Contents on large screens.
  no_toc:              false

  # When set to `true`, will not extend the content in the third column on large screens.
  no_third_column:     false

  # Set to `true` if you don't like oversized headlines on large screens.
  no_large_headings:   false

  # Set to `true` if you do not want to expose your resume and projects
  # in machine-readable formats.
  no_structured_data:  false

  # You can set this to `true` if you don't want to set the theme-color meta tag.
  no_theme_color:      false

  # Disable the breadcrumbs above the title
  no_breadcrumbs:      false

  # Set to `true` when building with the `--lsi` option.
  use_lsi:             true

  # When using Google Analytics, set to `true` to display a cookie notice banner.
  cookies_banner:      false

  # Set to `true` if you would like to add a "Powered by Hydejack" link in the footer.
  advertise:           false

  # PRO version setting
  hide_dates:          false

  # PRO version setting
  hide_last_modified:  false

  # Dark mode only works in the PRO version of Hydejack.
  dark_mode:
    always:            false
    dynamic:           true
    icon:              true

  # Experimental offline settings
  offline:
    enabled:           false
    cache_version:     13
    precache_assets:
      - /assets/img/swipe.svg


# Collections
# ----------------------------------------------------------------------------------------

collections:
  featured_categories:
    permalink:         /:name/
    output:            true

  featured_tags:
    permalink:         /tag-:name/
    output:            true

  projects:
    permalink:         /projects/:path/
    output:            true


# File inclusion/exclusion
# ----------------------------------------------------------------------------------------

exclude:
  - .jekyll-cache
  - .sass-cache
  - .ruby-lsp
  - .wrangler
  - '*.toml'
  - vendor
  - Gemfile
  - Gemfile.lock
  - functions

include:
  - _routes.json
  - .well-known
  - LICENSE.md


# Plugins
# ----------------------------------------------------------------------------------------

plugins:
  - jekyll-default-layout
  - jekyll-feed
  - jekyll-optional-front-matter
  - jekyll-paginate
  - jekyll-readme-index
  - jekyll-redirect-from
  - jekyll-relative-links
  - jekyll-seo-tag
  - jekyll-sitemap
  - jekyll-titles-from-headings
  - jekyll-include-cache
  - jekyll-remote-theme


# SEO Tag
# ---------------------------------------------------------------------------------------

# Where you prove that you own this site
# google_site_verification: <verification-id>
# webmaster_verifications:
#   google:              <verification-id>
#   bing:                <verification-id>
#   alexa:               <verification-id>
#   yandex:              <verification-id>

# Used for twitter cards
# twitter:
#   username:            <username>

# Used for facebook open graph
# facebook:
#   app_id:              <id>
#   publisher:           <id>
#   admins:              <id>

# Used on index and about sites
# social:
#   name:                <firstname> <lastname>
#   links:
#     - https://twitter.com/<username>
#     - https://github.com/<username>


# Plugin Configs
# ---------------------------------------------------------------------------------------

optional_front_matter:
  remove_originals:    true

readme_index:
  remove_originals:    true
  with_frontmatter:    true

relative_links:
  collections:         true

titles_from_headings:
  strip_title:         true
  collections:         true

compress_html:
  comments:            ["<!--", "-->"]
  clippings:           all
  endings:             all
  ignore:
    envs:              [development]

sass:
  style:               compressed


# Jekyll Compose Defaults
# ---------------------------------------------------------------------------------------

jekyll_compose:
  default_front_matter:
    drafts:
      layout: post
      description: >
        A short ~160 character description of your post for search engines,
        social media previews, etc.
      image:
        path: /assets/img/sidebar-bg.jpg

    posts:
      layout: post
      description: >
        A short ~160 character description of your post for search engines,
        social media previews, etc.
      image:
        path: /assets/img/sidebar-bg.jpg

    projects:
      layout: project
      description: >
        A short ~160 character description of your post for search engines,
        social media previews, etc.
      image:
        path: /assets/img/sidebar-bg.jpg
      links:
        - title: Project Link
          url: https://example.com
